spring.application.name=url-shortener-sb

# Render dynamic port support ($PORT environment variable assigned by Render)
server.port=${PORT:8080}

# Database configuration (Aiven PostgreSQL)
spring.datasource.url=${DB_URL:jdbc:postgresql://pg-14790c19-harmeettsinghh28-f7a7.j.aivencloud.com:23647/defaultdb?sslmode=require}
spring.datasource.username=${DB_USERNAME:avnadmin}
spring.datasource.password=${DB_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
#spring.jpa.show-sql=true

# HikariCP Connection Pool Optimization for Cloud Database
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000
spring.datasource.hikari.idle-timeout=30000
spring.datasource.hikari.max-lifetime=60000
spring.datasource.hikari.keepalive-time=30000

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration=${JWT_EXPIRATION:172800000}

# CORS Allowed Origins (Comma-separated, e.g. http://localhost:5173,https://your-frontend.vercel.app)
frontend.url=${FRONTEND_URL:http://localhost:5173}